# Server
PORT=3001

# CRE Workflow HTTP trigger URL (from `cre workflow deploy` output)
CRE_WORKFLOW_URL=http://localhost:8080/trigger

# EVM private key — same key as authorizedEVMAddress in CRE config
EVM_PRIVATE_KEY=0x...

# World ID
WORLD_APP_ID=app_YOUR_WORLD_APP_ID
WORLD_ACTION=verify-human
